@model TestNote.DAL.Models.NoteModel

@{
    ViewData["Title"] = "Home Page";
}
<div class="container">
    <form class="form-inline"
          asp-action="Index" asp-controller="Note" data-ajax-loading="#spinner"
          data-ajax="true"
          data-ajax-method="GET"
          data-ajax-success="LoadNodeSucces"
          data-ajax-update="#noteTable">

        <div class="input-group mb-3 mr-sm-3">
            <div class="date" id="datetimepicker1" data-target-input="nearest">
                <input type="text" name="startDate" class="form-control datetimepicker-input" data-target="#datetimepicker1" placeholder="FROM" required />
            </div>
            <div class="input-group-append" data-target="#datetimepicker1" data-toggle="datetimepicker">
                <div class="input-group-text"> <img src="~/icons/calendar.svg" alt="" title="from"></div>
            </div>
        </div>

        <div class="input-group mb-3 mr-sm-3">
            <div class="date" id="datetimepicker2" data-target-input="nearest">
                <input type="text" name="endDate" class="form-control datetimepicker-input" data-target="#datetimepicker2" placeholder="TO" required />
            </div>
            <div class="input-group-append" data-target="#datetimepicker2" data-toggle="datetimepicker">
                <div class="input-group-text"> <img src="~/icons/calendar.svg" alt="" title="to"></div>
            </div>
        </div>

        <button class="btn btn-outline-primary mb-3 mr-sm-3" type="submit">
            <img src="~/icons/search.svg" alt="" title="Search">
        </button>
    </form>
    <hr />

    <div class="row" id="myPartialContainer">
        @*<div id="myPartialContainer"></div>*@
        @*@await Html.PartialAsync("../Note/Create")*@
    </div>

    <div class="d-flex justify-content-center">
        <div class="spinner-border" role="status" id="spinner" style="display:none;">
            <span class="sr-only">Loading...</span>
        </div>
    </div>

    <div class="row" id="noteTable">
        @*<div class="col-md-4">
                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <p class="card-text">This is a wider card with supporting text below as a natural lead-in to additional content. This content is a little bit longer.</p>
                        <small class="text-muted">Username, 9 mins</small>
                    </div>
                </div>
            </div>*@
    </div>
</div>

@section Scripts{
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/signalr/signalr.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/tempusdominus-bootstrap-4/5.0.0-alpha14/js/tempusdominus-bootstrap-4.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tempusdominus-bootstrap-4/5.0.0-alpha14/css/tempusdominus-bootstrap-4.min.css" />
    <script>
        $(document).ready(function () {

            $('#datetimepicker1').datetimepicker();
            $('#datetimepicker2').datetimepicker({
                useCurrent: false //Important! See issue #1075
            });

            $("#datetimepicker1").on("change.datetimepicker", function (e) {
                $('#datetimepicker2').datetimepicker('minDate', e.date);
            });

            $("#datetimepicker2").on("change.datetimepicker", function (e) {
                $('#datetimepicker1').datetimepicker('maxDate', e.date);
            });

            $.ajax({
                url: '/Note/Create',
                dataType: 'html',
                success: function (data) {
                    $('#myPartialContainer').html(data);

                    var $form = $("#myPartialContainer").find("form");
                    //$form.validate();

                    $form.removeData("validator");
                    $form.removeData("unobtrusiveValidation");
                    $.validator.unobtrusive.parse($form);
                    //$form.unbind();
                    //$form.data("validator", null);
                    //$.validator.unobtrusive.parse(document);
                }
            });

            //$("#myPartialContainer").load("/Note/Create");
        });

    </script>
    <script src="~/js/signalr/hubs/note.js"></script>
    <script>
        function LoadNodeSucces(data) {
            var dvItems = $("#noteTable");
            dvItems.empty();
            $.each(data, function (i, item) {

                $('<div class="col-md-3">' +
                    '<div class="card mb-3 shadow-sm">' +
                    '<div class="card-body">' +
                    '<p class="card-text">' + item.content + '</p>' +
                    '<small class="text-muted">' + item.userName + ', ' + item.createDate + '</small>' +
                    '</div>' +
                    '</div>' +
                    '</div>').appendTo(dvItems);
            });
        }

        function CreateNodeSucces(data) {
            var dvItems = $("#noteTable");

            $('<div class="col-md-3">' +
                '<div class="card mb-3 shadow-sm">' +
                '<div class="card-body">' +
                '<p class="card-text">' + data.content + '</p>' +
                '<small class="text-muted">' + data.userName + ', ' + data.createDate + '</small>' +
                '</div>' +
                '</div>' +
                '</div>').appendTo(dvItems);

            var user = data.userName;
            var message = 'messageInput';
            connection.invoke("SendNotification", user, message).catch(function (err) {
                return console.error(err.toString());
            });
            event.preventDefault();
        }
    </script>
    @*<script>
            $.ajax({
            type: "GET",
            url: "/Note/Index",
            contentType: "application/json",
            dataType: "json",
            success: function (response) {
                var dvItems = $("#noteTable");
                dvItems.empty();
                $.each(response, function (i, item) {

                    $('<div class="col-md-4">' +
                        '<div class="card mb-4 shadow-sm">' +
                            '<div class="card-body">' +
                                '<p class="card-text">' + item.content + '</p>' +
                                '<small class="text-muted">' + item.userName + ', ' + item.createDate + '</small>' +
                            '</div>' +
                        '</div>' +
                    '</div>').appendTo(dvItems);
                    //var $tr = $('<li>').append(item.content).appendTo(dvItems);
                });
            },
            failure: function (response) {
                alert(response);
            }
        });
        </script>*@
}


<script>
                            //(function () {
                            //    'use strict';
                            //    window.addEventListener('load', function () {
                            //        var forms = document.getElementsByClassName('needs-validation');
                            //        var validation = Array.prototype.filter.call(forms, function (form) {
                            //            form.addEventListener('submit', function (event) {
                            //                if (form.checkValidity() === false) {
                            //                    event.preventDefault();
                            //                    event.stopPropagation();
                            //                }
                            //                form.classList.add('was-validated');
                            //            }, false);
                            //        });
                            //    }, false);
                            //})();
</script>
